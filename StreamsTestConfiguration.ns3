Newspeak3
'NSStreams'
class StreamsTestConfiguration packageTestsUsing: ns = (
(* Test configuration for StreamsTesting *)|
	Streams = ns Streams.
	StreamsTesting = ns StreamsTesting.
|) (
'as yet unclassified'
public testModulesUsingPlatform: platform <Platform> minitest: minitest <Minitest> ^<{NSStreamsTesting}> = (
	| streams |
	streams:: Streams usingPlatform: platform.
	^{StreamsTesting usingPlatform: platform testing: streams minitest: minitest}.
)
) : (
)
