Newspeak3
'Root'
class BuildInfo packageUsing: manifest = Object new (
	(* Build/version stamp for the Newspeak WebIDE. This committed copy is a
	placeholder; tool/build.sh regenerates it with the real git-derived version
	at build time and restores the placeholder afterward, so a deployed vfuel
	reports its true provenance while the working tree stays clean. *)
) (
) : (
	public version = (
		^'development build'
	)
)
